import { FC } from 'react';
import { SimpleGridProps } from '@chakra-ui/react';
export declare type ThemeEditorPaletteProps = {
    palette: Record<string | number, string>;
    scale?: (string | number)[];
    value?: string;
    token: string;
    showIndex?: boolean;
    disableEditDrawer?: boolean;
} & Omit<SimpleGridProps, 'onChange'>;
declare const ThemeEditorPalette: FC<ThemeEditorPaletteProps>;
export default ThemeEditorPalette;
