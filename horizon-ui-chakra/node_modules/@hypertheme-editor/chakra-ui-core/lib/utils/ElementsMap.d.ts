export interface ElementsMapEventData {
    toRenderElements: Record<string, HTMLElement[]>;
}
export declare class ElementsMap {
    private static instance;
    private handlers;
    cssVarElementsMap: Record<string, HTMLElement[]>;
    private _toRenderElements;
    private constructor();
    static getInstance(): ElementsMap;
    on(handler: (data: ElementsMapEventData) => void): void;
    off(handler: (data: ElementsMapEventData) => void): void;
    trigger(data: ElementsMapEventData): void;
    get toRenderElements(): Record<string, HTMLElement[]>;
    set toRenderElements(value: Record<string, HTMLElement[]>);
}
