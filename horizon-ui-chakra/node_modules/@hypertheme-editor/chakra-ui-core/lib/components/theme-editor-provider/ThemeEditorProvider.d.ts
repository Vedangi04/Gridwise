import React, { FC } from 'react';
declare type Props = {
    children: React.ReactNode | React.ReactNode[];
    disableGoogleFonts?: boolean;
};
export declare const ThemeEditorProvider: FC<Props>;
export {};
