import { FC, ReactNode } from 'react';
import { StackProps, As } from '@chakra-ui/react';
declare type Props = {
    icon?: As;
    title?: string;
    company?: string;
    product?: string;
    children?: ReactNode;
} & StackProps;
declare const CreditsBox: FC<Props>;
export default CreditsBox;
