import { StackProps, ThemingProps } from '@chakra-ui/react';
import { FC } from 'react';
declare type ColorModeToggleProps = {
    size?: 'sm' | 'md' | 'lg';
    showLabel?: boolean;
} & StackProps & ThemingProps;
declare const ColorModeToggle: FC<ColorModeToggleProps>;
export default ColorModeToggle;
