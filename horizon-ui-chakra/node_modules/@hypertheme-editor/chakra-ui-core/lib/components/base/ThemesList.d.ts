import { FC } from 'react';
import { StackProps } from '@chakra-ui/react';
declare type ThemeItem = {
    title: string;
    value: string;
    updated: Date;
    id: string;
};
declare type ThemesListProps = {
    themes: ThemeItem[];
    onChange?: (nextValue: string) => void;
    isEditable?: boolean;
    hasDelete?: boolean;
    onDelete?: (id: string) => void;
} & StackProps;
declare const ThemesList: FC<ThemesListProps>;
export default ThemesList;
