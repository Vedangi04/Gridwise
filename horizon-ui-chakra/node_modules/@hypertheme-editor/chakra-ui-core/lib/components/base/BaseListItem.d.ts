import React, { FC } from 'react';
import { ThemingProps, As, BoxProps } from '@chakra-ui/react';
export declare type BaseListItemProps = {
    subtitle?: string;
    bgImage?: string;
    icon?: As;
    endIcon?: React.ReactElement;
    isClickable?: boolean;
} & Pick<ThemingProps, 'colorScheme'> & BoxProps;
declare const BaseListItem: FC<BaseListItemProps>;
export default BaseListItem;
