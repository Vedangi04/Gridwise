import { FC, ReactNode } from 'react';
import { StackProps, As } from '@chakra-ui/react';
declare type Props = {
    icon?: As;
    title?: string;
    subtitle?: string;
    children?: ReactNode;
} & StackProps;
declare const EmptyBox: FC<Props>;
export default EmptyBox;
