import { FC, ReactNode } from 'react';
import { StackProps, As, ThemingProps } from '@chakra-ui/react';
declare type Props = {
    icon?: As;
    title?: string;
    subtitle?: string;
    children?: ReactNode;
} & StackProps & Pick<ThemingProps, 'colorScheme'>;
declare const UpgradeBanner: FC<Props>;
export default UpgradeBanner;
