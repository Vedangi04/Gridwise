import { FC } from 'react';
import { ThemingProps, MenuItemProps, As } from '@chakra-ui/react';
export declare type BaseMenuItemProps = {
    subtitle?: string;
    children?: any;
    bgImage?: string;
    icon?: As;
    endIcon?: As;
} & Omit<MenuItemProps, 'icon'> & Pick<ThemingProps, 'colorScheme'>;
declare const BaseMenuItem: FC<BaseMenuItemProps>;
export default BaseMenuItem;
