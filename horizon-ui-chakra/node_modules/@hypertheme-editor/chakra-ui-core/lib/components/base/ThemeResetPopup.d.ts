import { FC } from 'react';
import { StackProps, ModalProps } from '@chakra-ui/react';
declare type ThemeResetPopupProps = {
    isOpen: boolean;
    onReset?: () => void;
    onClose: () => void;
} & StackProps & ModalProps;
declare const ThemeResetPopup: FC<ThemeResetPopupProps>;
export default ThemeResetPopup;
