import { FC } from 'react';
import { SimpleGridProps, Theme } from '@chakra-ui/react';
export declare type ThemeIconProps = {
    theme?: Theme;
    colors?: {}[];
    size?: 'sm' | 'md' | 'lg';
} & SimpleGridProps;
declare const ThemeIcon: FC<ThemeIconProps>;
export default ThemeIcon;
