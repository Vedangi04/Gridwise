import { FC, ReactNode } from 'react';
import { As, ModalProps, ThemingProps } from '@chakra-ui/react';
declare type Props = {
    title?: string;
    description?: string;
    onConfirm: () => void;
    onClose: () => void;
    children?: ReactNode;
    confirmIcon?: As;
    avoidIcon?: As;
} & Omit<ModalProps, 'children'> & Pick<ThemingProps, 'colorScheme'>;
export declare const ConfirmModal: FC<Props>;
export {};
